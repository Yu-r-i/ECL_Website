server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  # Reactアプリ (seminar/build/) のSPA対応
  location /seminar/build/ {
    try_files $uri /seminar/build/index.html;
  }

  # 通常HTMLコンテンツ
  location / {
    try_files $uri $uri/ =404;
  }
}
